name: oech_app
description: "РЧ 2024 год"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  expandable_page_view: ^1.0.17
  flutter_svg: ^2.2.2
  pinput: ^5.0.2
  animated_toggle_switch: ^0.8.5
  load_switch: ^2.2.0
  flutter_switch: ^0.3.2
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.7
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/